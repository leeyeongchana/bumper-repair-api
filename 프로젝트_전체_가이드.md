# 범퍼 수정일지 자동화 시스템 — 전체 가이드

> KIA 광주 캠스 상주원 품질 현황 관리 시스템  
> 작성일: 2026-04-30

---

## 1. 파일 구조

```
bumper-repair-api/          ← GitHub 레포 (백엔드)
├── main.py                 ← FastAPI 서버 (핵심 백엔드)
├── requirements.txt        ← Python 패키지 목록
└── render.yaml             ← Render.com 배포 설정

netlify-frontend/           ← Netlify 배포 (프론트엔드)
├── index.html              ← 상주원 입력 앱 (모바일 최적화)
└── dashboard.html          ← PC 대시보드 (관리자용)
```

---

## 2. 배포 현황

| 서비스 | URL | 역할 |
|--------|-----|------|
| **백엔드 API** | https://bumper-repair-api.onrender.com | FastAPI + PostgreSQL |
| **상주원 입력앱** | https://splendid-eclair-fce5bc.netlify.app | 모바일 입력 |
| **대시보드** | https://splendid-eclair-fce5bc.netlify.app/dashboard.html | PC 현황 조회 |
| **DB** | Supabase PostgreSQL | 영구 데이터 저장 |
| **백엔드 호스팅** | Render.com (무료) | API 서버 |
| **프론트 호스팅** | Netlify (무료) | HTML 파일 서빙 |

---

## 3. 실행 방법 (신규 배포 시)

### 3-1. 백엔드 (Render.com)

```
1. github.com/leeyeongchana/bumper-repair-api 접속
2. main.py, requirements.txt 업로드
3. dashboard.render.com → bumper-repair-api → Environment
   → DATABASE_URL 설정:
   postgresql://postgres.mndwihiznivwfbxyufbj:[비밀번호]@aws-0-ap-northeast-2.pooler.supabase.com:5432/postgres
4. Manual Deploy 클릭 → "Your service is live 🎉" 확인
```

### 3-2. 프론트엔드 (Netlify)

```
1. app.netlify.com 접속
2. splendid-eclair-fce5bc 프로젝트 선택
3. Production deploys 섹션에
   index.html + dashboard.html 담긴 폴더 드래그앤드롭
4. "Deploy without renaming" 클릭
5. Published 확인
```

---

## 4. 필요한 패키지 (requirements.txt)

```
fastapi
uvicorn[standard]
psycopg2-binary
pydantic
```

---

## 5. 데이터베이스 초기 설정

### Supabase 프로젝트 정보
- **Project ID**: mndwihiznivwfbxyufbj
- **Region**: ap-northeast-2 (서울)
- **Connection**: Session Pooler (IPv4 호환)

### 테이블 자동 생성
`main.py`의 `init_db()` 함수가 서버 시작 시 자동으로 테이블을 생성합니다.

```sql
CREATE TABLE IF NOT EXISTS records (
    id           SERIAL PRIMARY KEY,
    factory      TEXT,    -- 공장 (1공장/2공장/3공장)
    model        TEXT,    -- 차종 (OV1/SP3/NQ5 PE/PU/C·PAD)
    position     TEXT,    -- 위치 (FRT/RR)
    cno          TEXT,    -- 차체번호
    color        TEXT,    -- 색상코드
    action       TEXT,    -- 조치 (수정/교환)
    defect       TEXT,    -- 불량유형
    defect_sub   TEXT,    -- 교환부품명
    resp         TEXT,    -- 귀책처 (캠스/기아/복합)
    memo         TEXT,    -- 메모
    markers      TEXT,    -- 마킹 데이터 (JSON)
    photos       TEXT,    -- 사진 (base64 JSON 배열)
    submitted_at TEXT,    -- 제출 시각
    date         TEXT     -- 날짜 (YYYY-MM-DD)
)
```

### 전체 데이터 삭제 방법
```
https://bumper-repair-api.onrender.com/docs
→ DELETE /records → Try it out → Execute
```

---

## 6. API 엔드포인트

| 메서드 | URL | 설명 |
|--------|-----|------|
| GET | `/health` | 서버 상태 확인 |
| POST | `/submit` | 수정일지 1건 제출 |
| GET | `/records?limit=5000` | 전체 기록 조회 |
| DELETE | `/records` | 전체 기록 삭제 |

### POST /submit 요청 형식
```json
{
  "factory": "1공장",
  "model": "OV1",
  "position": "FRT",
  "cno": "1234",
  "color": "KDG",
  "action": "수정",
  "defect": "찍힘",
  "defect_sub": "",
  "resp": "캠스",
  "memo": "",
  "markers": [],
  "photos": []
}
```

---

## 7. 동작 원리

```
[상주원 스마트폰]          [Render.com]           [Supabase DB]
  index.html         →    POST /submit      →    INSERT records
  (입력 후 제출)            FastAPI 서버            PostgreSQL

[관리자 PC]                [Render.com]           [Supabase DB]
  dashboard.html     →    GET /records      →    SELECT records
  (새로고침/필터)            FastAPI 서버            PostgreSQL
```

### 핵심 원리
- 모든 데이터는 **Supabase PostgreSQL**에 영구 저장
- 상주원 3명이 동시에 입력해도 **충돌 없음** (DB ACID 트랜잭션 보장)
- 대시보드는 최대 5,000건 한 번에 로드 후 **브라우저에서 필터링**
- Render.com 무료 플랜은 비활성 시 **슬립 상태** → 첫 요청 50초 지연 가능

---

## 8. 사진 저장 방식

- 상주원이 사진 첨부 시 **브라우저에서 자동 압축** (최대 1200px, 80% 품질)
- 압축된 이미지를 **base64 문자열**로 변환 후 DB 저장
- Supabase 무료 500MB 기준 **월 약 60MB** 사용 (사진 10장/일 기준)
- 유료 전환 시 **Supabase Storage** 방식으로 개선 예정 (원본 화질 유지)

---

## 9. 대시보드 주요 기능

### 필터
| 필터 | 옵션 |
|------|------|
| 기간 | 일별(날짜 선택) / 주간(시작~종료) / 월간 / 분기 / 연간 / 전체 |
| 공장 | 전체 / 1공장 / 2공장 / 3공장 |
| 조치 | 전체 / 수정 / 교환 |
| 귀책처 | 전체 / 캠스 / 기아 / 복합 |

### 탭
| 탭 | 내용 |
|----|------|
| 📊 대시보드 | KPI 카드, 공장별 차트, 불량유형, 조치사항, 책임구 |
| 📷 사진 기록 | 필터 기준 사진 있는 기록만 카드 형태로 표시 |
| 📋 상세 기록 | 전체 기록 테이블 (삭제 기능 포함) |

---

## 10. 주의사항

1. **Render.com 무료 플랜**: 15분 비활성 시 슬립 → 첫 요청 50초 대기
2. **비밀번호 특수문자 금지**: DATABASE_URL에 `@`, `#`, `!` 포함 시 연결 오류
3. **Netlify 업로드**: 반드시 **폴더째** 드래그앤드롭 (파일 개별 업로드 X)
4. **사진 DB 용량**: 500MB 한계 도달 시 유료 플랜 또는 Storage 방식 전환 필요
5. **데이터 백업**: 현재 별도 백업 기능 없음 → 중요 데이터는 주기적으로 CSV 추출 권장

---

## 11. 향후 개선 예정

- [ ] 유료 DB 전환 후 사진 원본 화질 저장 (Supabase Storage)
- [ ] 서버사이드 집계 API 추가 (대용량 데이터 성능 개선)
- [ ] 데이터 CSV/Excel 내보내기 기능
- [ ] 3공장 색상이전 앱 연동 (plant 3 샘플 파일 입수 후)
